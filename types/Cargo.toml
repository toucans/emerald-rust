[package]
name = "types"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "The seam: plain frame data the game emits, plus the Backend/AudioSink ports."

[dependencies]
