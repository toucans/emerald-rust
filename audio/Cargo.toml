[package]
name = "audio"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "The Rust m4a/MP2K engine: pure logic, score + samples in, 44.1 kHz float PCM out. Zero dependencies."

[dependencies]
